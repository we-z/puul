import SwiftUI

struct TempView: View {
    var body: some View {
        Text("Hello, World!")
    }
}


struct TempView_Previews: PreviewProvider {
    static var previews: some View {
        TempView()
    }
}
